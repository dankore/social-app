<%- include('includes/header') %>

<div class="container py-md-5 container--narrow">
  <%- include('includes/flash') %>
  
 <% if(posts.length){ %>
    <div class="list-group">
      <h2 class="text-center mb-4">The Latest From Those You Follow</h2>
    <% posts.forEach((post)=> { %>
    <a
      href="/post/<%= post._id %>"
      class="list-group-item list-group-item-action"
    >
      <img class="avatar-tiny" src="<%= post.author.avatar %>" />
      <strong><%= post.title %></strong> <span class="text-muted small">by <%=post.author.username %> on <%= post.createdDate.getMonth() + 1
      %>/<%= post.createdDate.getDate() %>/<%= post.createdDate.getFullYear() %></span>
    </a>

    <% }) %>
  </div>
<% }else{ %>
    <div class="text-center">
    <h2>Hello <a href="/profile/<%= user.username %>"><strong><%= user.username %></strong></a>, your feed is empty.</h2>
    <p class="lead text-muted">
      Your feed displays the latest posts from the people you follow. If you
      don&rsquo;t have any friends to follow that&rsquo;s okay; you can use the
      &ldquo;Search&rdquo; feature in the top menu bar to find content written
      by people with similar interests and then follow them.
    </p>
    <p class="text-monospace">To help connect you to those using this medium. Click on the search icon above, and type the word 
    "awesome." My post should appear in the search results. Click on the search item. After that, click on my picture or my username
    "dankore." Once there, click on the Following tab and follow everyone that I follow. I know this is not convenient but
    for now it is one way to discover others and enjoy the site. Cheers, Adamu! </p>
  </div>
 <% } %>
</div>

<%- include('includes/footer') %>
